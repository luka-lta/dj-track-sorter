name: Build

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        os: [macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install backend dependencies
        run: |
          python -m venv backend/.venv
          if [ -f backend/.venv/Scripts/pip.exe ]; then
            backend/.venv/Scripts/pip.exe install -r backend/requirements.txt
          else
            backend/.venv/bin/pip install -r backend/requirements.txt
          fi
        shell: bash

      - name: Run backend tests
        run: |
          if [ -f backend/.venv/Scripts/python.exe ]; then
            backend/.venv/Scripts/python.exe -m pytest backend -v
          else
            backend/.venv/bin/python -m pytest backend -v
          fi
        shell: bash

      - name: Install frontend dependencies
        run: npm ci

      - name: Build sidecar + package app
        run: npm run dist
        shell: bash

      - uses: actions/upload-artifact@v4
        with:
          name: dj-track-sorter-${{ matrix.os }}
          path: |
            dist/*.dmg
            dist/*.exe
          if-no-files-found: ignore

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
